[server]
# Pin the Python version to 3.10, which is highly stable and supported by all your dependencies.
# This forces the build environment to use a compatible Python runtime.
enableCORS = true
enableXsrfProtection = true

[logger]
level = "info"
